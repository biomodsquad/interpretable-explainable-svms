# MISTIC validation studies

This directory contains synthetic-data notebooks, helper scripts, generated
tables, and figures used to validate MISTIC's feature-selection behavior.

These studies are kept separate from `mistic/examples`, which contains the
smaller user-facing package examples. Run notebooks with this directory as the
working directory so their relative result-file paths resolve alongside the
notebooks.
